# FinanzTracker – Clean Cloudflare Worker project

Upload this exact folder structure to GitHub.

Root files:
- worker.js
- wrangler.jsonc
- package.json

Public website files are inside `public/`.

Cloudflare build settings:
- Build command: empty
- Deploy command: npx wrangler deploy
- Root directory: empty

Do not put `node_modules` or ZIP files into the repository.
